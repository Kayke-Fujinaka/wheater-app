import styled from 'styled-components'

export const RealTime = styled.h3`
  font-weight: var(--normal);
  font-size: var(--Small-font-size);
  color: var(--text-grey-color);
  margin-bottom: 1.875rem;
`
